# MyRaices v19.1.2 — Hero CMS Image Fix

- El Hero ahora usa un elemento `<img>` real en lugar de depender exclusivamente de `background-image`.
- La URL publicada desde NURAI se asigna directamente al atributo `src`.
- Se mantienen imagen desktop/móvil, posición, texto alternativo y fallback local.
- No se modifican checkout, catálogo, autenticación, pedidos, Square, Brevo ni Supabase.
