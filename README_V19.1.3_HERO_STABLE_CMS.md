# MyRaices v19.1.3 — Stable CMS Hero

- Rebuilt the homepage Hero media layer using one real `<img>` element.
- The image published from NURAI updates the image `src` directly.
- Desktop and mobile image URLs remain supported.
- `/assets/hero-hand-plant.webp` is loaded by default and used automatically if the CMS image fails.
- Removed the Hero's dependency on competing CSS background layers.
- The built-in content is applied immediately while Supabase content loads.
- No commerce, authentication, inventory, checkout, Square, Brevo, blog, or order logic was changed.
