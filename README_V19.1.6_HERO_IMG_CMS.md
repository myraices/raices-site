# MyRaíces v19.1.6 — Hero IMG CMS

- Reconstruye el Hero con un elemento `<picture><img>` real.
- La imagen local `/assets/hero-hand-plant.webp` se muestra antes de cargar el CMS.
- NURAI cambia `src` y `srcset` para escritorio y móvil.
- Elimina la dependencia de `background-image` y de los estilos heredados `.hero-art`.
- La hoja `hero-cms-v19.1.6.css` se carga al final para aislar el Hero de reglas antiguas.
