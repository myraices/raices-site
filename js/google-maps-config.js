/* Raíces — Google Maps browser configuration
   The browser key is injected by Netlify during the build from the
   GOOGLE_MAPS_API_KEY environment variable. Do not paste a real key here.
   Google Maps browser keys are visible in the browser by design and must be
   protected with HTTP referrer and API restrictions in Google Cloud. */
window.RAICES_GOOGLE_MAPS_API_KEY = "__GOOGLE_MAPS_API_KEY__";
