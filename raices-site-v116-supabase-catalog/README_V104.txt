RAÍCES v104 — Google Address Autocomplete fix

This version replaces the new PlaceAutocompleteElement with the stable Google Maps Places Autocomplete input.

IMPORTANT:
1. Open js/google-maps-config.js
2. Paste the restricted Google Maps browser API key between the quotes.
3. Save, commit, and push.
4. Test only on https://myraices.com because the key is restricted to that domain.

The exposed key from v103 must still be rotated after testing.
